import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'E-learningbook\t',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
